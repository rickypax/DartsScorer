import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'develop.pax.dartscorer',
  appName: 'Darts Scorer',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
